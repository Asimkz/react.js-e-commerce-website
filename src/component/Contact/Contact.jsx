import React from 'react';

import PageNotFound from '../PageNotFound/PageNotFound';
const Contact= (props) => {
    return (
        <>
            <PageNotFound/>
        </>
    );
};

export default Contact;